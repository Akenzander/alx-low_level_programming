#include "main.h"
/**
 * _strlen - return the length of a string
 * @s: the string pointer
 * Return: index
 */
int _strlen(char *s);
{
	int index;
	{

		for (index = 0; s(index) != '\0'; index++)
	}
			return (index);
}
