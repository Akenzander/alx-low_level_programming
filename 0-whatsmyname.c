#include "main.h"
#include <stdio>

/**
 * main - entry point
 * description: prints the name of the program
 * @argc:number of command line arguements
 * @argv:an array of command line argurements
 * Return: 0
 */

int main(int argc, char *argv[])
	{
		printf("%s\n", argv[argc * 0]);

		return (0);
	}
